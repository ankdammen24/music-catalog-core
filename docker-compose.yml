services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file: .env
    ports: ["4000:4000"]
  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file: .env
    command: npm run worker
