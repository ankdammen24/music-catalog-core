version: '3.9'
services:
  api:
    image: node:22-alpine
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: sh -c "corepack enable && pnpm install && pnpm dev"
    ports:
      - "3000:3000"
    env_file:
      - .env
