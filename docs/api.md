# API
Health, users, artists, albums, tracks, releases, playlists, uploads endpoints are provided under root paths.

For frontend integration contract (soundloom-core), see `docs/frontend-contract.md`.
