{
  "name": "music-catalog-core",
  "private": true,
  "version": "0.1.0",
  "description": "Central backend monorepo for Media Rosenqvist music catalog",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @mrq/api dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
