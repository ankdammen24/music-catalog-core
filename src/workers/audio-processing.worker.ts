import { mkdtempSync, writeFileSync, readFileSync, rmSync } from "node:fs"; import { join } from "node:path"; import { tmpdir } from "node:os";
import { GetObjectCommand, PutObjectCommand } from "@aws-sdk/client-s3";
import { env } from "../config/env.js"; import { r2Client, buildFlacKey } from "../services/r2.service.js"; import { audioService } from "../services/audio.service.js"; import { supabase } from "../db/supabase.js";
async function streamToBuffer(stream:any){const chunks:Buffer[]=[]; for await (const c of stream) chunks.push(Buffer.from(c)); return Buffer.concat(chunks);} 
async function tick(){ const {data:jobs}=await supabase.from("processing_jobs").select("*").eq("status","queued").limit(1); const job=jobs?.[0] as any; if(!job)return; const temp=mkdtempSync(join(tmpdir(),"mc-"));
try{ await supabase.from("processing_jobs").update({status:"running"}).eq("id",job.id); const res=await r2Client.send(new GetObjectCommand({Bucket:env.R2_BUCKET,Key:job.input_r2_key})); const inFile=join(temp,"in"); writeFileSync(inFile, await streamToBuffer(res.Body)); const probe=await audioService.probe(inFile); const loud=await audioService.analyzeLoudness(inFile); const outFile=join(temp,`${job.track_id}.flac`); await audioService.toFlac(inFile,outFile); const outKey=buildFlacKey(job.organization_id,job.track_id); await r2Client.send(new PutObjectCommand({Bucket:env.R2_BUCKET,Key:outKey,Body:readFileSync(outFile),ContentType:"audio/flac"})); await supabase.from("tracks").update({status:"processed",duration_seconds:Math.round(probe.duration),sample_rate:probe.sampleRate,channels:probe.channels,loudness_lufs:loud.lufs,true_peak_db:loud.truePeakDb,audio_flac_r2_key:outKey,audio_master_r2_key:outKey}).eq("id",job.track_id); await supabase.from("processing_jobs").update({status:"completed",output_r2_key:outKey,log:{probe,loud}}).eq("id",job.id);
}catch(e:any){await supabase.from("processing_jobs").update({status:"failed",error_message:e.message}).eq("id",job.id); await supabase.from("tracks").update({status:"rejected"}).eq("id",job.track_id);} finally {rmSync(temp,{recursive:true,force:true});}}
if(env.AUDIO_WORKER_ENABLED){setInterval(()=>void tick(),5000);} 
